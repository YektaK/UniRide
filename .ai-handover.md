# Devir Raporu (AI Handover)

Bu dosya, asistanlar arası (veya seanslar arası) bilgi aktarımını sağlamak için kullanılır. Her mesai bitiminde asistan bu dosyayı YÜKSEK detayla günceller. Asistan ilk işe başladığında `.ai-rules` dosyasındaki sıra uyarınca `docs/01_Implementation_Status.md` vs. dosyalarından sonra en son bu dosyayı okur.

---

## 🕒 Tarih: 10.04.2026 01:38 (10.04.2026 - Ekleyen: Copilot AI)

## ✅ Bu Seans Tamamlananlar

### A-3: Sandbox IE endpoint düzeltme (tam çözüm)
`/api/v1/ie/analyze` endpoint'i `main.py`'de hiçbir zaman tanımlanmamıştı. `/api/v1/optimize` endpoint'i zaten ResourceProfiler ile IE analizini inline yapıyor ve sonucu `result.ie_data` olarak döndürüyor (main.py:401).

`sandbox/route.ts`'de yapılan tam düzeltme:
- Var olmayan IE fetch kaldırıldı
- `depot` nesnesi eklendi (`DEFAULT_DEPOT = Doğuş Üniversitesi`)  
- `vehicles` Python `VehicleConfig` şemasına dönüştürüldü: `swCapacity → sw_capacity`, `soCapacity → so_capacity`
- `students` Python `StudentNode` şemasına dönüştürüldü (snake_case + camelCase her ikisi de destekleniyor)
- `result.ie_data` (raw Python `IERawData`) → `ieData` (frontend `IEResponseData`) dönüştürme helper'ı (`transformIEData`) eklendi
- Frontend sandbox UI `data.data?.ieData` veya `data.ieData` alanını arıyor; artık doğru şekilde sağlanıyor

### C-2: `kmeans_tsp.py` arşivleme
K-Means TSP stratejisi hiçbir zaman gerçek implementasyon içermiyordu (sadece dummy sequential routing). Kullanıcı K-Means'ın gelecek versiyonlarda kullanılmayacağını belirtti. Dosya `optimizer_api/strategies/_archived/` dizinine taşındı. `strategies/__init__.py` docstring güncellendi.

### C-4: Supabase env var boş string fallback kaldırma
`config.ts`'deki `SUPABASE_URL || ""`, `SUPABASE_ANON_KEY || ""`, `SUPABASE_SERVICE_ROLE_KEY || ""` export'ları kaldırıldı. Bu sabitler hiçbir yerde import edilmiyordu. Gerçek doğrulama `supabase.ts` (warn + null client → throw on use) ve `supabase-admin.ts` (immediate throw) tarafından yapılıyor.

---

## 🕒 Tarih: 10.04.2026 01:08 (10.04.2026 - Ekleyen: Copilot AI)

## ✅ Bu Seans Tamamlananlar

### C-3: `as any` kaldırma — `admin/users/route.ts`

**Kök neden:**
`Database.users.Row/Insert/Update` `DbUser` (camelCase uygulama modeli) olarak tanımlıydı, ancak gerçek Supabase tablosu snake_case kolonlar kullanıyor. Bu uyumsuzluk tüm `adminClient` nesnesini `as any` ile atlatmak zorunda bırakıyordu.

**Yapılan değişiklikler:**
1. **`src/types/db.ts`** — `DbUserRow` interface eklendi. Gerçek DB şemasıyla (`supabase/schema.sql`) birebir eşleşen snake_case alanlar. Ne zaman `DbUserRow`, ne zaman `DbUser` kullanılacağı dokümante edildi.
2. **`src/lib/supabase.ts`** — `Database.users.Row/Insert/Update` `DbUser`'dan `DbUserRow`'a güncellendi.
3. **`src/app/api/admin/users/route.ts`**:
   - `(adminClient as any)` kaldırıldı (2 adet — satır 86 ve 144)
   - `dbUpdates: any` → `Partial<Omit<DbUserRow, "id">>` ile değiştirildi
   - Insert payload `Partial<DbUserRow>` değişkeni olarak çıkarıldı
   - Supabase method boundary'de `as never` kullanıldı (kod tabanı genelinde kullanılan kalıpla tutarlı: `supabase-db.ts:153`, `profile/password/route.ts:47`)
   - `home_address: homeAddress ?? null` — `|| ""` yerine `?? null` kullanıldı (tutarlılık)

**Bonus:** `auth/hint/route.ts:78`'deki bilinen baseline typecheck hatası da bu değişiklikle düzeldi (`password_hint` artık `DbUserRow`'da mevcut).

**Typecheck sonucu:** 0 hata (önceden 1 tane bilinen baseline hata vardı).

## ⚠️ Açık Sorunlar

1. **A-3: Sandbox IE analizi çalışmıyor** — `sandbox/route.ts:95`: `/api/v1/ie/analyze` endpoint'i `main.py`'de tanımlı değil.
2. **C-2: `kmeans_tsp.py` ölü kod** — Registry'e kayıtlı değil.
3. **C-4: Supabase env var boş string fallback** — Startup'ta hata fırlatmalı.

## 🎯 Bir Sonraki Seans Önerilen İlk 3 İş

1. **A-3** — `optimizer_api/main.py`'ye `/api/v1/ie/analyze` ekle (kaynak: `resource_profiler.py`)
2. **C-2** — `kmeans_tsp.py`'yi registry'e kaydet veya arşive taşı
3. **P11** — SOTA benchmark: `requirements-benchmark.txt` ile PyVRP/VROOM kurulumu ve benchmark çalıştırma

---

## ✅ Tamamlananlar

### Kapsamlı Statik Kod Analizi
- Tüm aktif kaynak kodu (`/src`, `/optimizer_api`) ve dokümantasyon taranarak 11 sorun tespit edildi.
- **Güvenlik sorunları** (CR-1, CR-3), **fonksiyonel bug'lar** (CR-4, CR-8, CR-9), **teknik borç** (CR-6, CR-7) önceliklendirildi.
- Her sorun dosya + satır numarasıyla belgelendi.

### Yeni Dokümanlar
- `docs/09_04_2026_Codebase_Analysis_Report.md` — Kapsamlı analiz raporu (11 sorun)
- `docs/05_Code_Quality_Roadmap.md` — Öncelikli düzeltme yol haritası (paralel çalışma rehberi dahil)

### Güncellenen Dokümanlar
- `README.md` — 15+ algoritma listesi, tam endpoint tablosu (7), bağımlılık notu
- `docs/02_Architecture.md` — React 19→18, Tailwind 4→3 versiyonu düzeltildi; strateji dizin ağacı genişletildi; rate-limit iddiası düzeltildi
- `docs/03_Roadmap.md` — GWO/HHO/TW görevleri ✅ olarak işaretlendi; Faz 4.4 kod kalitesi tablosu eklendi
- `docs/01_Implementation_Status.md` — Güvenlik/fonksiyonel düzeltmeler ve bekleyen görevler bölümü eklendi
- `.ai-rules` — `docs/05` ve `docs/09_04` zorunlu okuma listesine eklendi (madde 5 ve 6)

### Kod Düzeltmeleri

| Commit | Değişiklik | Neden Önemli |
|---|---|---|
| `5d87418` | `requireAdmin` → `POST /api/calculate-vehicles` | Kimliği doğrulanmamış herkes pahalı optimizasyon tetikleyebiliyordu |
| `fcf4ce1` | `total_time_window_violations: Optional[int]` → `OptimizationResponse` | 4 split strateji bu alanı modele geçiriyordu, Pydantic v2'de ValidationError veya silent drop |
| `b33c646` | `sandbox/route.ts`: `strategy`→`algorithm`, `max_tour_time`→`max_travel_time` | Sandbox her zaman varsayılan algoritmayı ve 120 dk'yı kullanıyordu |
| `1bf2b97` | `ALLOWED_ORIGINS` env var → `optimizer_api/main.py` | CORS wildcard production'da güvenlik açığıydı |
| `config.ts` | TODO yorumu → `RATE_LIMIT_REQUESTS_PER_MINUTE` | Yanıltıcı export; genel rate limiting uygulandığı izlenimi veriyordu |

## ⚠️ Açık Sorunlar

1. **A-3: Sandbox IE analizi hâlâ çalışmıyor** — `src/app/api/sandbox/route.ts:95` `OPTIMIZER_API_URL + "/api/v1/ie/analyze"` çağrısı yapıyor ama bu endpoint `main.py`'de tanımlı değil. Hata sessizce yutulur. İki çözüm yolu: (a) endpoint ekle, (b) `/api/v1/optimize` yanıtındaki `ie_data` alanını kullan.

2. **C-2: `kmeans_tsp.py` ölü kod** — Registry'e kayıtlı değil, kullanılmıyor. Kaydet veya kaldır.

3. **Bilinen Baseline Typecheck Hatası** — `src/app/api/auth/hint/route.ts:78`: `password_hint` key type mismatch. Bu bu oturumdan ÖNCE mevcuttu, bu oturumda dokunulmadı.

## 💡 Teknik Borçlar

- `as any` kullanımı 11 yerde devam ediyor (en önceliklisi: `admin/users/route.ts:86,144`)
- `rateLimitMap` in-process Map; çok process'li deployment'larda Redis ile değiştirilmeli
- `optimizeRoutes` `time_window_size` gönderir ama Python API bu alanı bilmez (sessizce yok sayılır)
- `kmeans_tsp.py` stratejisi registry'e eklenmeli

## 🎯 Bir Sonraki Seans Odaklanılacak İlk 3 İş

1. **A-3: Sandbox IE Endpoint** — Ya `/api/v1/ie/analyze`'ı `optimizer_api/main.py`'ye ekle (kaynak: `resource_profiler.py`) ya da sandbox'ın `/api/v1/optimize` yanıtındaki `ie_data` alanını kullan.
2. **P11: SOTA Benchmark** — `requirements-benchmark.txt` ile PyVRP/VROOM kurulumu ve benchmark çalıştırma.
3. **C-2: kmeans_tsp.py** — Registry'e kaydet (`__init__.py`'ye ekle) veya arşive taşı.

---

## ✅ Tamamlananlar:
- **SOTA Çözücü Analizi:** PyVRP, VROOM ve OR-Tools çözücülerinin mevcut durumu analiz edildi. Kodların hazır olduğu ancak `requirements.txt`'de tanımlı olmadığı tespit edildi.
- **Dokümantasyon Güncellemesi:** 
  - `.ai-rules`: Tarih/ekleyen bilgisi zorunluluğu ve silme yasağı (görüş sistemi) eklendi
  - `docs/ALGORITHM_COMPARISON.md`: Bölüm 6 - SOTA Çözücü Entegrasyon Durumu eklendi
  - `docs/sota_framework_plan_2026/01_SOTA_Architecture_Vision.md`: Bölüm 3 - SOTA Baseline Çözücüler eklendi
  - `docs/01_Implementation_Status.md`: P11 görevi eklendi
- **Akademik Gerekçe:** Makalede SOTA iddiası için PyVRP ile kıyaslamanın zorunlu olduğu belgelendi

## ⚠️ Açık Sorunlar:
- PyVRP ve VROOM `requirements.txt`'de tanımlı değil, kurulu değilse sessizce fallback yapılıyor
- Benchmark STRATEGIES listesine SOTA çözücüler eklenmedi

## 💡 Teknik Borçlar (Technical Debt):
- (03.04.2026 - Önceki:) ALNS operatörleri henüz implement edilmedi
- (04.04.2026 - Z.ai:) SOTA çözücülerin benchmark entegrasyonu bekliyor (P11)

## 🎯 Bir Sonraki Seans Odaklanılacak İlk İş:
1. **P11: SOTA Çözücü Benchmark Entegrasyonu** - `requirements.txt`'ye pyvrp/pyvroom ekle ve benchmark'a entegre et
2. ALNS (Destroy/Repair) operatörlerini tasarlamaya başlamak
3. Benchmark sonuçlarını gerçek verilerle güncellemek

---

## 🕒 Tarih: 03.04.2026 05:05

## ✅ Tamamlananlar:
- **Dokümantasyon Altyapısı Kökten Revize Edildi:** Karmaşıklık yaratan blueprint ve eski md dosyaları `Gereksiz/` arşivine taşındı.
- **Akademik Makale (Performance Comparison) Vizyonu Kurulumsallaştırıldı:** Geliştiricinin referans alması için SOTA Dual-Track belgeleri `docs/sota_framework_plan_2026/01_SOTA_Architecture_Vision.md` altına işlendi. `.ai-rules` makale hedefi için kural ihlallerini önlemek adına sıkılaştırıldı.
- **Canlı ve Algoritma Bağlantıları Restore Edildi:** `schemas.py` içerisindeki SOTA bypass flag'leri ve kayıp `allow_time_shift` configleri asıl koda entegre edildi. `1schemas.old.py` arşive kaldırıldı.
- **Smart Benchmark (Akıllı Önbellek & Terminal Dashboard) Canlıda!** 
  - `academic_benchmark` izole test ortamı kuruldu.
  - TSPLib haritalarının yanına konan her `.opt.tour` dosyasını uçtan uca okuyup Euclid formülüyle gerçek optimum mesafeyi dinamik / otomatik çıkartan `dataset_loader.py` arayüzü yazıldı.
  - Algoritma scriptleri değiştirildiğinde SHA256 Hash takibi yapan ve tüm PC'yi tıkamaktan koruyan `run_smart_benchmark.py` (Interactive Seçim Ekranı) uygulaması geliştirildi. Eski skorlar JSON Metadata ve CSV formatında arşivlendi.

## ⚠️ Açık Sorunlar:
- Şu an test edilen algoritmaların sadece çözme süresi ve yüzdelik Gap çıktılarını logluyoruz. Rota çıktısı, kıyaslama yapabilmemiz için `.opt.tour` verisiyle yanyana getirilmiyor. İleride güzergah benzerliği de çalışılacaksa bu loglama işlevi genişletilmelidir.

## 💡 Teknik Borçlar (Technical Debt):
- Ticari ürün için `Hybrid Local Search` standart kalmaya devam ediyor ancak makale vizyonu için ALNS (Large Neighborhood Search) tabanlı gelişmiş "Destroy / Repair" operatörleri henüz SOTA Engine'e işlenmedi.

## 🎯 Bir Sonraki Seans Odaklanılacak İlk İş:
1. SOTA vizyonumuzun (Makale çalışmasının) ikinci adımı olan **ALNS (Yok Et ve Onar) operatörlerini** tasarlamaya başlamak. `local_search.py` veya bağımsız bir SOTA modülü üzerinde TSPLib için Destroy/Repair metodolojisini inşa edip, Smart Benchmark arayüzünden eski Hybrid/2-Opt algoritmalarımıza karşı test etmek.
