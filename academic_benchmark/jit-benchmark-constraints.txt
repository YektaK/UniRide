# Dedicated constraints for the isolated academic JIT benchmark environment.
#
# Use as a pip constraints file, for example:
#   python -m pip install -e ".[test]" -c academic_benchmark/jit-benchmark-constraints.txt
#
# This profile requires Python >=3.11,<3.15 because NumPy 2.4.6 requires
# Python >=3.11. Numba 0.66.0 supports NumPy >=2.0,<2.5 and llvmlite 0.48.x.
# NumPy 2.4.6 is the latest non-yanked release in that range at publication.
numpy==2.4.6
llvmlite==0.48.0
numba==0.66.0
