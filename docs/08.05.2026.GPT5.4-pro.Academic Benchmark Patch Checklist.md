Academic Benchmark Patch Checklist
==================================

**Date:** 2026-05-08  
**Author:** GitHub Copilot  
**Purpose:** File-by-file implementation checklist for the issues identified in the benchmark review
1. master_sota_engine.py
------------------------

1. Fix resumed run indexing in the default engine path.
2. Use effective_run_idx rather than local loop index for seed generation.
3. Persist run_index explicitly in raw result rows.
4. Include the effective config or params in result rows so params_json is not empty in progress output.
5. Populate the active-results buffer used by the SIGINT path.
6. Emit a true run-level result file separate from aggregate summaries.
7. Keep benchmark_summary.csv as a post-aggregation artifact only.
8. Add a test for partial cache resume continuity.
9. Add a test that interrupted execution writes non-empty interrupted CSV content.
10. Validate that SOTA tuning outputs are emitted with discoverable metadata for the dashboard.

2. master_numba_engine.py
-------------------------

1. Change benchmark progress output so raw runs and aggregate summaries are separate artifacts.
2. Keep aggregate rows out of any file that the dashboard will treat as run-level observations.
3. Harmonize problem loading behavior with the SOTA loader.
4. Replace strict fallback utf-8 reads with tolerant reads.
5. Use merge-and-deduplicate loading even when archive loading only partially succeeds.
6. Persist a manifest of loaded problems and their source type.
7. Populate the active-results buffer used by the SIGINT path.
8. Add a parity test that compares Numba-discovered problem names with SOTA-discovered problem names under the same fixture setup.
9. Validate that all dashboard-facing outputs now match the shared data contract.

3. dashboard.py
---------------

1. Replace flat tuning-file discovery with recursive or explicit nested-path discovery.
2. Separate ingestion by result type:
   * run-level data
   * aggregate summaries
   * tuning results
3. Remove the assumption that all loaded progress rows are raw multi-run data.
4. Restrict box plots and robustness views to run-level observations only.
5. Add engine, result_type, and file_origin fields to loaded frames.
6. Add a diagnostics panel showing loaded files, row counts, and missing expected sources.
7. Replace unconditional external CSV caching with a refresh button, TTL, or file-mtime-aware strategy.
8. Validate that SOTA tuning data appears correctly once nested discovery is implemented.
9. Validate that Numba and SOTA plots no longer mix aggregates with raw runs.
10. Add a reproduction test or manual check for LaTeX export behavior with missing optimal values before treating NaN handling as a confirmed defect.

4. r2dma_tsp.py
---------------

1. Review the resonance components for cyclic-tour semantics.
2. Canonicalize tours before position-based comparison or redesign those metrics around cycle structure.
3. Reassess weights after canonicalization so edge similarity remains meaningful but not overwhelming.
4. Add unit tests for:
   * identical tours
   * rotated equivalent tours
   * reversed equivalent tours if intended
   * structurally unrelated tours
5. Re-run solver smoke tests after changing resonance semantics.

5. paoea_tsp.py
---------------

1. Replace prefix-based destroy-operator mutation with random subset sampling.
2. Consider applying the same improvement to repair-operator mutation so both operator families have comparable exploration diversity.
3. Keep operator weights aligned with the selected operator subset.
4. Add tests that mutation produces varied operator combinations across repeated trials.
5. Re-run deterministic-seed tests to ensure the mutation update remains reproducible.

6. ls_engine.py
---------------

1. Restore iterative convergence behavior rather than a single-pass-only layered search.
2. Add a bounded repeated-improvement loop with time or iteration guards.
3. Extend Numba-backed execution to cover 3-opt and swap if corresponding accelerated kernels already exist.
4. Add size-aware pruning for larger instances.
5. Consider candidate-list-based 2-opt or Or-opt to reduce unnecessary neighborhood scans.
6. Consider applying the full local-search stack only to elites, accepted improvements, or stagnated individuals.
7. Add tests that demonstrate multiple passes can improve beyond one pass.
8. Validate quality-runtime tradeoffs on small, medium, and large TSPLIB groups.

7. bildiri2026
--------------

1. Verify the current public nb_three_opt implementation.
2. If it delegates to 2-opt, replace it with a true 3-opt path or deprecate/rename it.
3. Add a regression test that distinguishes nb_three_opt behavior from nb_two_opt on a known case.
4. Audit any callers that assume true 3-opt semantics.

8. local_search_numba.py
------------------------

1. Review the Numba cache configuration.
2. If cache is forced off globally, evaluate safer alternatives such as per-process temporary cache directories.
3. Document the rationale for the chosen cache policy.
4. Confirm whether accelerated 3-opt and swap kernels exist and are production-ready for ls_engine integration.

9. master_sota_engine.py and master_numba_engine.py
---------------------------------------------------

1. Define a shared output schema document for both engines.
2. Standardize field names for:
   * strategy
   * problem
   * run_index
   * seed
   * tour_cost or avg_length
   * gap_pct or avg_gap
   * elapsed_ms
   * params_json
   * engine
   * mode
   * result_type
3. Decide whether benchmark_progress.csv should be retired, repurposed, or maintained as a compatibility shim.
4. Add a compatibility note if downstream scripts already depend on current filenames.

10. test_benchmark_robustness.py
--------------------------------

1. Extend this file or a sibling test file to cover resume continuity for SOTA runs.
2. Add coverage for output contract expectations.
3. Add loader parity checks if this file remains the benchmark-infrastructure test home.
4. Add an interrupted-run persistence test if practical.
5. Add a benchmark-integration test for run-level versus aggregate-level output semantics.

11. test_sota_e2e.py
--------------------

1. Add resonance invariance checks for rotated tours after R2DMA changes.
2. Keep the current solver-validity baseline tests.
3. Add one or two structural tests for multi-pass local-search improvement if runtime allows.
4. Add or reference a test for any revised 3-opt semantics if it lands in shared Numba codepaths.

12. test_sota_parity.py
-----------------------

1. Keep the current parity-oriented checks.
2. Add a targeted test for full-intensity local search if layer order or repeated-improvement behavior changes.
3. Add a focused P-AOEA mutation-diversity check if mutation logic is refactored.

13. test_problem_selector.py
----------------------------

1. No major issue found here during this review.

2. Keep this file as a reference for selection-behavior stability.

3. Only expand it if loader parity or problem-manifest logic introduces new selection-facing behavior.

4. Performance Investigation Track

-----------------------------------

1. Verify whether local-search wrapper classes outside academic_benchmark rebuild distance matrices on every improve call.

2. If confirmed, cache or reuse prepared distance matrices where safe.

3. Measure the impact before treating it as a primary engineering priority.

4. Suggested Patch Order

-------------------------

1. Patch master_sota_engine.py for resume integrity.

2. Patch interruption persistence in both master engines.

3. Patch true-or-deprecated nb_three_opt behavior.

4. Patch ls_engine.py for convergence semantics.

5. Patch raw-run versus aggregate output separation in both engines.

6. Patch dashboard.py to consume the new data contract.

7. Patch master_numba_engine.py for loader parity.

8. Patch r2dma_tsp.py for cyclic-tour semantics.

9. Patch paoea_tsp.py for operator mutation diversity.

10. Patch ls_engine.py for extended Numba acceleration and scaling improvements.

11. Review Numba cache policy.

12. Add or update tests to lock in behavior.

13. Finish with docs and compatibility notes.

14. Completion Checklist

------------------------

1. Resume path verified.
2. Interrupted-run persistence verified.
3. nb_three_opt is no longer semantically misleading.
4. MultiLayerLS convergence behavior is explicit and tested.
5. Raw and aggregate outputs separated.
6. Dashboard updated for the new contract.
7. SOTA tuning visible in UI.
8. R2DMA resonance semantics improved.
9. Numba/SOTA loader parity validated.
10. Regression tests added and passing.
