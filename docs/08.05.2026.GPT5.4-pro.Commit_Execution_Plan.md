Commit-by-Commit Execution Plan
===============================

**Date:** 2026-05-08  
**Author:** GitHub Copilot  
**Purpose:** Safe implementation order that fixes correctness first, then data contracts, then UI, then solver refinements
Commit 1: Fix SOTA resume integrity and run indexing
----------------------------------------------------

**Summary:** Repair resumed run indexing and seed continuity in the SOTA default benchmark path.

**Files:**

* master_sota_engine.py
* test_benchmark_robustness.py or a new focused infrastructure test

**Changes:**

1. Introduce effective_run_idx based on saved_runs.
2. Use effective_run_idx in seed generation and raw result rows.
3. Add a regression test for partial cache resume.

**Validation:**

1. Resume test passes.
2. Seed sequence after partial cache matches expected continuation.
3. No existing solver tests regress.

* * *

Commit 2: Populate active-results buffers and harden interruption persistence
-----------------------------------------------------------------------------

**Summary:** Make SIGINT persistence paths actually store completed rows before interruption.

**Files:**

* master_sota_engine.py
* master_numba_engine.py

**Changes:**

1. Append completed rows to active-results buffers.
2. Ensure both engines flush those rows on interruption.
3. Add interruption-oriented tests if practical.

**Validation:**

1. Simulated interruption yields non-empty interrupted CSV output.
2. Metadata remains consistent.
3. Row counts reflect actually completed tasks.

* * *

Commit 3: Implement or deprecate misleading nb_three_opt behavior
-----------------------------------------------------------------

**Summary:** Ensure the public 3-opt path is semantically truthful.

**Files:**

* bildiri2026
* any related Numba or wrapper tests

**Changes:**

1. Verify the current public nb_three_opt implementation.
2. If it delegates to 2-opt, replace it with a true 3-opt path or deprecate and rename it.
3. Add a semantic regression test.

**Validation:**

1. nb_three_opt no longer trivially aliases nb_two_opt behavior.
2. Caller expectations match actual neighborhood semantics.
3. Existing Numba-path callers still work.

* * *

Commit 4: Restore MultiLayerLS convergence semantics
----------------------------------------------------

**Summary:** Bring layered local search back to bounded iterative improvement rather than a one-pass-only structure.

**Files:**

* ls_engine.py
* test_sota_parity.py or a focused LS test file

**Changes:**

1. Introduce repeated-improvement behavior with time or iteration guards.
2. Preserve the current intensity model.
3. Add a test where multiple passes improve beyond one pass.

**Validation:**

1. Multi-pass test passes.
2. Existing solver-validity tests still pass.
3. Runtime remains bounded.

* * *

Commit 5: Introduce a shared run-level output contract
------------------------------------------------------

**Summary:** Separate raw-run observations from aggregate summary rows across both engines.

**Files:**

* master_sota_engine.py
* master_numba_engine.py

**Changes:**

1. Define a shared run-level file format.
2. Keep benchmark_summary.csv aggregate-only.
3. Add fields such as run_index, seed, engine, mode, and result_type.
4. Preserve old files temporarily only if compatibility is needed.

**Validation:**

1. Both engines emit compatible run-level files.
2. Summaries remain intact.
3. Run-level rows are sufficient for downstream robustness analysis.

* * *

Commit 6: Update dashboard loading and robustness logic
-------------------------------------------------------

**Summary:** Teach the dashboard to consume the new output contract correctly and discover nested tuning outputs.

**Files:**

* dashboard.py

**Changes:**

1. Load raw-run files, summaries, and tuning data separately.
2. Discover nested tuning locations such as the SOTA tuning directory.
3. Restrict robustness charts to raw-run observations.
4. Add source diagnostics and refresh-safe caching behavior.
5. Add a reproduction check for LaTeX export behavior with missing optimal values.

**Validation:**

1. SOTA tuning data appears in the DoE tab.
2. Box plots only use raw-run observations.
3. Diagnostics panel shows loaded sources and row counts.
4. Refresh behavior is understandable to the user.

* * *

Commit 7: Harmonize Numba loader behavior with SOTA
---------------------------------------------------

**Summary:** Make Numba problem loading merge fallback data safely and match SOTA corpus behavior.

**Files:**

* master_numba_engine.py
* master_sota_engine.py only if shared helper extraction is useful
* test_benchmark_robustness.py

**Changes:**

1. Use merge-and-deduplicate loading in Numba.
2. Use tolerant fallback reads.
3. Persist a problem manifest or at least log loaded names and sources.
4. Add a cross-engine loader parity test.

**Validation:**

1. Numba and SOTA load the same normalized problem-name set under the same fixtures.
2. Partial archive degradation does not produce silent cross-engine drift.
3. Manifest output is visible and reviewable.

* * *

Commit 8: Normalize R2DMA resonance semantics for cyclic tours
--------------------------------------------------------------

**Summary:** Improve R2DMA resonance so equivalent cyclic tours are not penalized by arbitrary rotation.

**Files:**

* r2dma_tsp.py
* test_sota_e2e.py

**Changes:**

1. Canonicalize tours or redesign rotation-sensitive resonance dimensions.
2. Add tests for rotated-tour equivalence behavior.
3. Reassess weighting after semantic correction.

**Validation:**

1. New invariance tests pass.
2. Existing solver validity tests still pass.
3. Deterministic seed behavior remains stable.

* * *

Commit 9: Extend Numba-backed coverage in LS engine
---------------------------------------------------

**Summary:** Make full-intensity local search benefit from Numba more consistently.

**Files:**

* ls_engine.py
* local_search_numba.py if integration is required

**Changes:**

1. Add Numba-backed paths for 3-opt and swap if kernels exist and are stable.
2. Pass dm_np through those layers.
3. Measure before and after runtime impact.

**Validation:**

1. Accelerated paths are actually reachable.
2. Full-intensity local search is faster on representative cases.
3. Quality does not regress.

* * *

Commit 10: Improve P-AOEA mutation diversity
--------------------------------------------

**Summary:** Remove prefix bias from operator mutation and improve exploration diversity.

**Files:**

* paoea_tsp.py
* test_sota_parity.py or a focused PAOEA mutation test

**Changes:**

1. Replace prefix-based selection with random subset mutation.
2. Keep operator weights aligned to selected operators.
3. Add mutation-diversity regression tests.

**Validation:**

1. Mutation can produce varied operator families.
2. Seeded runs remain deterministic.
3. No solver-validity regressions appear.

* * *

Commit 11: Review Numba cache policy and runtime overhead
---------------------------------------------------------

**Summary:** Recover avoidable Numba runtime overhead without reintroducing multiprocessing instability.

**Files:**

* local_search_numba.py
* any wrapper or integration files implicated by runtime profiling

**Changes:**

1. Confirm the current cache policy.
2. Test per-process temporary cache directories if cache is globally disabled.
3. Investigate repeated distance-matrix rebuilds if profiling still points to them.

**Validation:**

1. JIT reuse improves where safe.
2. Multiprocessing remains stable.
3. Runtime profiling shows measurable improvement or documents why the current policy must remain.

* * *

Commit 12: Final regression and docs pass
-----------------------------------------

**Summary:** Consolidate tests and document the new data contract and benchmark behavior.

**Files:**

* test_benchmark_robustness.py
* test_sota_e2e.py
* test_sota_parity.py
* docs files of your choice

**Changes:**

1. Ensure all integrity-critical regressions are covered.
2. Document the shared output schema.
3. Document dashboard expectations and tuning-file discovery behavior.
4. Record which secondary-audit claims were confirmed and which remained investigation items.

**Validation:**

1. All targeted regression tests pass.
2. The docs match the implemented file contract.
3. The benchmark stack is now reproducible, inspectable, and easier to extend.

* * *

Recommended stopping points
---------------------------

If you want safe merge checkpoints, the best pause points are:

1. After Commit 2 for correctness and persistence.
2. After Commit 4 for resume, interruption, and solver-semantic truthfulness.
3. After Commit 6 for data-contract and dashboard consistency.
4. After Commit 8 for the main algorithm-semantic fix.
5. After Commit 12 for a final stable benchmark-platform state.
