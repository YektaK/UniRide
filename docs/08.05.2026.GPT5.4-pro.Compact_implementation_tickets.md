Compact Implementation Ticket List
==================================

**Date:** 2026-05-08  
**Author:** GitHub Copilot  
**Scope:** Academic benchmark integrity, dashboard correctness, solver semantics, and regression coverage
Ticket 1: Fix SOTA resume seed continuity
-----------------------------------------

**Goal:** Make resumed default benchmark runs continue missing run indices instead of restarting at zero.

**Primary file:** master_sota_engine.py

**Tasks:**

1. Compute effective_run_idx = saved_runs + local_run_idx.
2. Use effective_run_idx when generating seeds.
3. Persist run_index in raw result rows and metadata.

**Acceptance criteria:**

1. A partially cached 5-run job with 3 cached runs only executes run indices 3 and 4.
2. Seed generation is deterministic and monotonic across resumed runs.
3. Aggregated statistics do not duplicate earlier stochastic trajectories.

* * *

Ticket 2: Implement or deprecate misleading nb_three_opt behavior
-----------------------------------------------------------------

**Goal:** Ensure the public 3-opt API is semantically truthful.

**Primary area:** bildiri2026

**Tasks:**

1. Verify the current public nb_three_opt implementation.
2. If it delegates to 2-opt, replace it with a true 3-opt path or deprecate it.
3. Add a semantic regression test.

**Acceptance criteria:**

1. nb_three_opt is no longer a misleading alias.
2. Callers receive the neighborhood class implied by the API.
3. Tests distinguish its behavior from nb_two_opt or document its deprecation clearly.

* * *

Ticket 3: Restore MultiLayerLS convergence behavior
---------------------------------------------------

**Goal:** Make layered local search capable of repeated improvement rather than one pass only.

**Primary file:** ls_engine.py

**Tasks:**

1. Add bounded repeated-improvement semantics.
2. Preserve the intensity model.
3. Add a test where multiple passes improve beyond one pass.

**Acceptance criteria:**

1. Multi-pass improvement is demonstrably possible.
2. Runtime remains bounded.
3. Existing solver-validity tests still pass.

* * *

Ticket 4: Split raw-run outputs from aggregate summaries
--------------------------------------------------------

**Goal:** Stop mixing run-level data and aggregate benchmark rows.

**Primary files:**

* master_sota_engine.py
* master_numba_engine.py

**Tasks:**

1. Introduce a shared run-level output file.
2. Keep benchmark_summary.csv aggregate-only.
3. Add result_type, engine, mode, run_index, and seed fields where appropriate.

**Acceptance criteria:**

1. Both engines emit one consistent run-level schema.
2. Aggregate summaries are stored separately.
3. No dashboard chart depends on aggregate rows when it needs raw runs.

* * *

Ticket 5: Repair dashboard data ingestion
-----------------------------------------

**Goal:** Make the dashboard load the right data from the right locations.

**Primary file:** dashboard.py

**Tasks:**

1. Discover tuning data from nested engine-specific paths.
2. Separate loading for raw-run files, summaries, and tuning results.
3. Add source metadata such as engine, file_origin, and result_type.

**Acceptance criteria:**

1. SOTA tuning output is visible in the DoE tab.
2. Numba and SOTA raw-run data appear in robustness views without semantic mixing.
3. The dashboard can display which files it loaded.

* * *

Ticket 6: Restrict robustness charts to true raw-run observations
-----------------------------------------------------------------

**Goal:** Ensure statistical charts are academically valid.

**Primary file:** dashboard.py

**Tasks:**

1. Remove reliance on generic progress files for robustness charts.
2. Feed box plots only from run-level observations.
3. Add warnings when only aggregate data is available.

**Acceptance criteria:**

1. Box plots reflect independent trials only.
2. The UI no longer treats aggregated averages as raw observations.
3. Missing raw-run data is surfaced explicitly to the user.

* * *

Ticket 7: Normalize R2DMA resonance for cyclic tours
----------------------------------------------------

**Goal:** Improve resonance scoring so equivalent tours are not penalized by arbitrary start position.

**Primary file:** r2dma_tsp.py

**Tasks:**

1. Canonicalize tours before position-based comparison or redesign those dimensions around cycle invariants.
2. Rebalance weights if needed after normalization.
3. Add invariance tests for rotated tours.

**Acceptance criteria:**

1. Rotated equivalent tours score materially closer to identical tours than unrelated tours.
2. Solver behavior remains deterministic under fixed seeds.
3. Existing solver-validity tests still pass.

* * *

Ticket 8: Improve Numba loader parity and resilience
----------------------------------------------------

**Goal:** Make Numba load the same effective corpus as SOTA under the same local data conditions.

**Primary files:**

* master_numba_engine.py
* master_sota_engine.py

**Tasks:**

1. Use merge-and-deduplicate loading in Numba.
2. Tolerate fallback-file encoding irregularities.
3. Persist a manifest of loaded problem names and sources.

**Acceptance criteria:**

1. Numba and SOTA discover the same normalized problem set from the same fixture data.
2. Partial archive degradation does not silently shrink the Numba corpus.
3. Problem-source composition is visible in persisted outputs.

* * *

Ticket 9: Complete interrupted-run persistence
----------------------------------------------

**Goal:** Ensure interrupted runs persist actual completed results, not just metadata.

**Primary files:**

* master_sota_engine.py
* master_numba_engine.py

**Tasks:**

1. Append completed rows to the active-results buffer.
2. Flush the buffer to interrupted CSV output on SIGINT.
3. Add an interruption-focused regression test.

**Acceptance criteria:**

1. Interrupted CSV files contain actual completed rows.
2. Row count matches the number of finished tasks before interruption.
3. Metadata and interrupted CSV outputs agree on completion state.

* * *

Ticket 10: Extend Numba acceleration coverage in LS engine
----------------------------------------------------------

**Goal:** Make full-intensity local search benefit from Numba consistently.

**Primary file:** ls_engine.py

**Tasks:**

1. Add Numba-backed paths for 3-opt and swap if kernels exist.
2. Pass dm_np through those layers.
3. Benchmark runtime impact before and after.

**Acceptance criteria:**

1. Accelerated paths are actually reachable.
2. Full-intensity local search is faster on representative cases.
3. Quality does not regress.

* * *

Ticket 11: Improve P-AOEA operator mutation diversity
-----------------------------------------------------

**Goal:** Remove prefix bias from operator mutation.

**Primary file:** paoea_tsp.py

**Tasks:**

1. Replace prefix-based operator selection with random subset sampling.
2. Keep operator weights aligned to selected operators.
3. Add mutation-diversity tests.

**Acceptance criteria:**

1. Mutation can produce varied destroy and repair operator combinations.
2. Deterministic seeds still reproduce identical runs.
3. No solver-validity regression is introduced.

* * *

Ticket 12: Review Numba cache strategy
--------------------------------------

**Goal:** Prevent avoidable JIT overhead without reintroducing multiprocessing instability.

**Primary file:** local_search_numba.py

**Tasks:**

1. Confirm the current cache policy.
2. Evaluate per-process temporary cache directories if cache is globally disabled.
3. Document the rationale for the chosen policy.

**Acceptance criteria:**

1. The cache policy is intentional and documented.
2. JIT reuse improves where safe.
3. Multiprocessing behavior remains stable.

* * *

Ticket 13: Lock behavior with regression coverage
-------------------------------------------------

**Goal:** Prevent the reviewed issues from reappearing.

**Primary test files:**

* test_benchmark_robustness.py
* test_sota_e2e.py
* test_sota_parity.py

**Tasks:**

1. Add resume continuity tests.
2. Add loader parity tests.
3. Add resonance invariance tests.
4. Add interrupted-run persistence tests.
5. Add output-contract tests for dashboard-facing files.
6. Add nb_three_opt semantic-truthfulness tests.
7. Add MultiLayerLS multi-pass improvement tests.

**Acceptance criteria:**

1. All integrity-critical paths have direct regression tests.
2. Failures clearly identify contract violations.
3. Future refactors cannot silently reintroduce the same issues.

* * *

Ticket 14: Investigate repeated distance-matrix rebuilds
--------------------------------------------------------

**Goal:** Verify and eliminate avoidable matrix rebuild costs if they are on the active runtime path.

**Primary area:** optimizer-side local-search wrappers

**Tasks:**

1. Profile the active benchmark path.
2. Confirm whether distance matrices are rebuilt per improve call.
3. Cache or reuse prepared matrices where safe.

**Acceptance criteria:**

1. The issue is either ruled out or fixed with evidence.
2. Runtime overhead is reduced where the issue is confirmed.
3. The optimization does not change correctness.
